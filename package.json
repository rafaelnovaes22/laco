{
  "name": "debora-site",
  "version": "0.1.0",
  "private": true,
  "description": "Site institucional premium para Deborah Silva, cuidadora em movimento.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.js",
    "start": "node src/server.js",
    "test": "vitest run",
    "lint": "echo \"Sem lint padrão definido neste escopo.\""
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "supertest": "^6.3.4",
    "vitest": "^2.1.8"
  }
}
